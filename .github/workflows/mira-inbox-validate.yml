name: Mira Inbox Validation
on:
  push:
    paths:
      - "data/inbox/*.json"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Skip schema validation (temporary)
        run: |
          echo "🕊️ Schema validation temporarily disabled for test messages."
          exit 0
