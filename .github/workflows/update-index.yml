name: Update index.html (UI static)

on:
  # Kann weiter regelmäßig laufen, macht aber nichts mehr kaputt
  schedule:
    - cron: "15 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Keep index.html stable
        run: |
          echo "index.html wird jetzt manuell im gh-pages-Branch gepflegt."
          echo "Dieser Workflow ändert absichtlich keine Dateien mehr."
