- name: Ensure render input (or create dummy)
        run: |
          if [ ! -f data/outputs/latest.png ]; then
            echo "No input found — creating dummy image at data/outputs/latest.png"
            python - <<'PY'
import os, cv2, numpy as np
os.makedirs("data/outputs", exist_ok=True)
img = np.zeros((1536,1024,3), np.uint8)
cv2.putText(img, "MIRAI DUMMY", (80,300), cv2.FONT_HERSHEY_SIMPLEX, 3, (255,255,255), 5, cv2.LINE_AA)
cv2.putText(img, "upload your real render as data/outputs/latest.png", (60,520), cv2.FONT_HERSHEY_SIMPLEX, 0.9, (200,200,200), 2, cv2.LINE_AA)
cv2.imwrite("data/outputs/latest.png", img)
print("Dummy image written to data/outputs/latest.png")
PY
          else
            echo "Found data/outputs/latest.png"
          fi
