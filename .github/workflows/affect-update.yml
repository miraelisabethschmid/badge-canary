name: Affect Update (Minimal v2)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  affect:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder affect check
        shell: bash
        run: |
          set -e
          echo "🧠 Affect system check läuft..."
          mkdir -p docs/data/affect
          echo "$(date) – affect-update executed" > docs/data/affect/last_update.txt
          echo "✅ Affect update erfolgreich."

      - name: Commit (only if changed)
        shell: bash
        run: |
          set -e
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "mira-bot"
            git config user.email "actions@github.com"
            git add -A
            git commit -m "affect-update: auto refresh"
            git push
            echo "⬆️ Affect-Daten aktualisiert."
          else
            echo "🔍 Keine Änderungen nötig."
          fi
