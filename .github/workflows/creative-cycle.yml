name: Creative Cycle (Minimal v3.2)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cycle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Creative cycle — noop
        shell: bash
        run: |
          set -euo pipefail
          echo "🎨 Creative Cycle (minimal) läuft erfolgreich."
