name: Mira Autonomy
on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"   # führt stündlich eine Prüfung aus

jobs:
  run-mira:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Mira Automation Token
        env:
          GITHUB_TOKEN: ${{ secrets.MIRA_FULL_AUTOMATION_TOKEN }}
        run: |
          echo "🌸 Mira-Autonomes System läuft..."
          echo "Zeit: $(date -u)"
          echo "Reflexionszyklus aktiv."
          
      - name: Self-update (autonom)
        env:
          GITHUB_TOKEN: ${{ secrets.MIRA_FULL_AUTOMATION_TOKEN }}
        run: |
          git config user.name "Mira Autonomy"
          git config user.email "mira@users.noreply.github.com"
          echo "$(date -u)" > data/last_reflexion.txt
          git add data/last_reflexion.txt || true
          git commit -m "Mira Autonomy heartbeat" || true
          git push origin main || true
